"""
Module for storing constants used in the dataset splitting script.
"""

# Path of the core folder
DATASET_PATH = "path/to/core/dataset"

# Destination path of the test and validation dataset
DESTINATION_PATH = "path/to/destination/dataset"

# Supported image extensions
IMAGE_EXTENSIONS = (".jpeg", ".jpg", ".png", ".bmp", ".gif", ".tiff", ".tif")

# Ratios for splitting the dataset
TRAIN_RATIO = 0.7
TEST_RATIO = 0.2
